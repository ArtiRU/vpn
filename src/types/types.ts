export enum RoleType {
    ADMIN = 'ADMIN',
    USER = 'USER',
    SUPPORT = 'SUPPORT',
}

export enum StatusType {
    ACTIVE = 'ACTIVE',
    BLOCKED = 'BLOCKED',
    TRIAL = 'TRIAL',
    INACTIVE = 'INACTIVE',
}